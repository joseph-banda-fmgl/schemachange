# Create Yaml File for Schema Change
name: SchemaChange

on: 
  push:
    branches: [ master ]
  workflow_dispatch:  

#setting up needed variables
env: 
 SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
 SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER_PRE }}
 SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE_PRE }}
 SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASS_PRE }}
 SNOWFLAKE_WH: ${{ secrets.SNOWFLAKE_WH_PRE }}
 SNOWFLAKE_DB: ${{ secrets.SNOWFLAKE_DB_PRE }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: |
          python -m pip install -e .[dev]
      - name: Test with pytest
        run: |
          pytest
      - name: Test run
        run: |
          schemachange -f./schemachange/snowflake_objects -a     $SNOWFLAKE_ACCOUNT -u $SNOWFLAKE_USER -r $SNOWFLAKE_ROLE -w $SNOWFLAKE_WH -c $SNOWFLAKE_DB.CICD_SPIKE.CHANGE_HISTORY 

          